version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 10

    commands:
      - npm install
      - npm run build

artifacts:
  files:
    - favicon.ico
    - index.html
    - images/*
    - styles/*
    - dist/bundle.js
